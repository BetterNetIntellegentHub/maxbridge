# Deprecated: GitLab pipeline is disabled after migration to SourceCraft-only CI/CD.
# Source of truth: .sourcecraft/ci.yaml

stages:
  - deprecated

gitlab_deprecated:
  stage: deprecated
  script:
    - echo "GitLab CI is deprecated for this repository."
    - echo "Use SourceCraft workflows from .sourcecraft/ci.yaml"
