# Deprecated: GitLab Migration Runbook

This repository no longer uses GitLab as CI/CD source of truth.

Current CI/CD source of truth:
- `.sourcecraft/ci.yaml`

For migration and operations use:
- `docs/sourcecraft-migration.md`
