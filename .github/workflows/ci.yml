name: ci (deprecated)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: GitHub CI is disabled
        run: |
          echo "GitHub CI workflows are disabled."
          echo "Use SourceCraft workflows (.sourcecraft/ci.yaml) for CI/CD checks."
