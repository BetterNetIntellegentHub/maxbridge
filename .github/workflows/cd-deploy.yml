name: cd-deploy

on:
  workflow_run:
    workflows: ["cd-image"]
    types: [completed]
    branches: [main]
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Container tag override (example: sha-abc123def456 or v1.2.3); empty = auto sha tag"
        type: string
        required: false
        default: ""
      promote_to_production:
        description: "Deploy to production after successful staging"
        type: boolean
        required: true
        default: true

permissions:
  contents: read

concurrency:
  group: cd-deploy-main
  cancel-in-progress: false

jobs:
  prepare:
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.resolve.outputs.image_tag }}
      rollback_tag: ${{ steps.resolve.outputs.rollback_tag }}
      promote_to_production: ${{ steps.resolve.outputs.promote_to_production }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
          ref: ${{ github.event_name == 'workflow_run' && github.event.workflow_run.head_sha || github.sha }}

      - name: Resolve deployment tags
        id: resolve
        run: |
          set -euo pipefail

          if [ "${GITHUB_EVENT_NAME}" = "workflow_run" ]; then
            deploy_sha="${{ github.event.workflow_run.head_sha }}"
            deploy_tag="sha-${deploy_sha::12}"
            promote_to_production="true"
          else
            input_tag="${{ github.event.inputs.image_tag }}"
            if [ -n "${input_tag}" ]; then
              deploy_tag="${input_tag}"
            else
              deploy_tag="sha-${GITHUB_SHA::12}"
            fi
            promote_to_production="${{ github.event.inputs.promote_to_production }}"
          fi

          previous_sha="$(git rev-list --first-parent --max-count=2 HEAD | tail -n 1 || true)"
          rollback_tag="main"
          if [ -n "${previous_sha}" ] && [ "${previous_sha}" != "$(git rev-parse HEAD)" ]; then
            rollback_tag="sha-${previous_sha::12}"
          fi

          {
            echo "image_tag=${deploy_tag}"
            echo "rollback_tag=${rollback_tag}"
            echo "promote_to_production=${promote_to_production}"
          } >> "${GITHUB_OUTPUT}"

  deploy-staging:
    needs: prepare
    runs-on: [self-hosted, Linux, X64, wsl-deploy]
    environment: staging
    env:
      DEPLOY_ENV: staging
      IMAGE_TAG: ${{ needs.prepare.outputs.image_tag }}
      IMAGE_REPO: ${{ vars.MAXBRIDGE_IMAGE_REPO }}
      MAXBRIDGE_REGISTRY_USERNAME: ${{ vars.MAXBRIDGE_REGISTRY_USERNAME }}
      MAXBRIDGE_DOMAIN: ${{ vars.MAXBRIDGE_DOMAIN }}
      MAXBRIDGE_HTTPS_PORT: ${{ vars.MAXBRIDGE_HTTPS_PORT }}
      DEPLOY_HOST: ${{ vars.DEPLOY_HOST }}
      DEPLOY_USER: ${{ vars.DEPLOY_USER }}
      DEPLOY_SSH_KEY_PATH: ${{ vars.DEPLOY_SSH_KEY_PATH }}
      DEPLOY_SSH_KNOWN_HOSTS_PATH: ${{ vars.DEPLOY_SSH_KNOWN_HOSTS_PATH }}
      MAXBRIDGE_AUTO_BOOTSTRAP: ${{ vars.MAXBRIDGE_AUTO_BOOTSTRAP }}
      MAXBRIDGE_TELEGRAM_BOT_TOKEN: ${{ secrets.MAXBRIDGE_TELEGRAM_BOT_TOKEN }}
      MAXBRIDGE_MAX_BOT_TOKEN: ${{ secrets.MAXBRIDGE_MAX_BOT_TOKEN }}
      MAXBRIDGE_REGISTRY_TOKEN: ${{ secrets.MAXBRIDGE_REGISTRY_TOKEN }}
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Validate deploy configuration
        run: |
          set -euo pipefail

          missing=0
          for var_name in IMAGE_REPO DEPLOY_HOST DEPLOY_USER IMAGE_TAG MAXBRIDGE_DOMAIN MAXBRIDGE_TELEGRAM_BOT_TOKEN MAXBRIDGE_MAX_BOT_TOKEN MAXBRIDGE_REGISTRY_TOKEN; do
            if [ -z "${!var_name}" ]; then
              echo "Missing required value: ${var_name}"
              missing=1
            fi
          done

          if [ "${missing}" -ne 0 ]; then
            exit 1
          fi

          if [ "${IMAGE_TAG}" = "main" ]; then
            echo "image_tag=main is not allowed for deploy. Use immutable tags only (sha-<12hex> or v*)."
            exit 1
          fi

          if [[ ! "${IMAGE_TAG}" =~ ^sha-[0-9a-f]{12}$ && ! "${IMAGE_TAG}" =~ ^v[0-9A-Za-z._-]+$ ]]; then
            echo "Unsupported image_tag format: ${IMAGE_TAG}"
            echo "Allowed formats: sha-<12hex> or v* release tag."
            exit 1
          fi
      - name: Validate local SSH material on self-hosted runner
        run: |
          set -euo pipefail
          ssh_key_path="${DEPLOY_SSH_KEY_PATH:-$HOME/.ssh/maxbridge_deploy}"
          known_hosts_path="${DEPLOY_SSH_KNOWN_HOSTS_PATH:-$HOME/.ssh/known_hosts}"

          if [ ! -f "${ssh_key_path}" ]; then
            echo "Deploy SSH key is missing on runner: ${ssh_key_path}"
            exit 1
          fi

          if [ ! -f "${known_hosts_path}" ]; then
            echo "known_hosts file is missing on runner: ${known_hosts_path}"
            exit 1
          fi

          chmod 600 "${ssh_key_path}"
          chmod 600 "${known_hosts_path}"

          echo "SSH_KEY_PATH=${ssh_key_path}" >> "${GITHUB_ENV}"
          echo "KNOWN_HOSTS_PATH=${known_hosts_path}" >> "${GITHUB_ENV}"

      - name: Ensure toolchain is available
        run: |
          set -euo pipefail

          if ! command -v jq >/dev/null 2>&1; then
            echo "jq is required on self-hosted runner."
            exit 1
          fi

          if ! command -v ansible-playbook >/dev/null 2>&1; then
            echo "ansible-playbook is missing. Pre-provision Ansible on the self-hosted runner."
            exit 1
          fi

          if ! command -v ansible-galaxy >/dev/null 2>&1; then
            echo "ansible-galaxy is missing. Install full Ansible distribution on the self-hosted runner."
            exit 1
          fi

          ansible-galaxy collection install community.docker

      - name: Build runtime vars for Ansible
        run: |
          set -euo pipefail

          echo "::add-mask::${MAXBRIDGE_TELEGRAM_BOT_TOKEN}"
          echo "::add-mask::${MAXBRIDGE_MAX_BOT_TOKEN}"
          echo "::add-mask::${MAXBRIDGE_REGISTRY_TOKEN}"

          runtime_vars_file="${RUNNER_TEMP}/ansible-runtime-vars.json"
          https_port="${MAXBRIDGE_HTTPS_PORT:-8443}"
          registry_username="${MAXBRIDGE_REGISTRY_USERNAME}"

          if [ -z "${registry_username}" ]; then
            first_segment="${IMAGE_REPO%%/*}"
            if [[ "${first_segment}" == *.* || "${first_segment}" == *:* || "${first_segment}" == "localhost" ]]; then
              remainder="${IMAGE_REPO#*/}"
              registry_username="${remainder%%/*}"
            else
              registry_username="${first_segment}"
            fi
          fi

          if [ -z "${registry_username}" ] || [ "${registry_username}" = "${IMAGE_REPO}" ]; then
            echo "Unable to resolve registry username. Set MAXBRIDGE_REGISTRY_USERNAME variable."
            exit 1
          fi

          jq -n \
            --arg version "${IMAGE_TAG}" \
            --arg image "${IMAGE_REPO}:${IMAGE_TAG}" \
            --arg domain "${MAXBRIDGE_DOMAIN}" \
            --arg https_port "${https_port}" \
            --arg health_url "https://${MAXBRIDGE_DOMAIN}:${https_port}/health/ready" \
            --arg registry_username "${registry_username}" \
            --arg telegram_token "${MAXBRIDGE_TELEGRAM_BOT_TOKEN}" \
            --arg max_token "${MAXBRIDGE_MAX_BOT_TOKEN}" \
            --arg registry_token "${MAXBRIDGE_REGISTRY_TOKEN}" \
            '{
              maxbridge_version: $version,
              maxbridge_image: $image,
              maxbridge_domain: $domain,
              maxbridge_https_port: ($https_port | tonumber),
              maxbridge_healthcheck_url: $health_url,
              maxbridge_registry_private: true,
              maxbridge_registry_username: $registry_username,
              maxbridge_telegram_bot_token: $telegram_token,
              maxbridge_max_bot_token: $max_token,
              maxbridge_registry_token: $registry_token
            }' > "${runtime_vars_file}"

          chmod 600 "${runtime_vars_file}"
          echo "RUNTIME_VARS_FILE=${runtime_vars_file}" >> "${GITHUB_ENV}"

      - name: Render CI inventory
        run: |
          set -euo pipefail
          cat > "${RUNNER_TEMP}/hosts.yml" <<EOF_HOSTS
          all:
            hosts:
              maxbridge-${DEPLOY_ENV}-1:
                ansible_host: ${DEPLOY_HOST}
                ansible_user: ${DEPLOY_USER}
                ansible_ssh_private_key_file: ${SSH_KEY_PATH}
                ansible_ssh_common_args: '-o StrictHostKeyChecking=yes -o UserKnownHostsFile=${KNOWN_HOSTS_PATH}'
          EOF_HOSTS

      - name: Optional bootstrap
        if: ${{ vars.MAXBRIDGE_AUTO_BOOTSTRAP == 'true' }}
        env:
          ANSIBLE_HOST_KEY_CHECKING: "true"
        run: |
          set -euo pipefail
          ansible-playbook \
            -i "${RUNNER_TEMP}/hosts.yml" \
            deploy/ansible/bootstrap.yml

      - name: Deploy image with Ansible
        env:
          ANSIBLE_HOST_KEY_CHECKING: "true"
        run: |
          set -euo pipefail
          ansible-playbook \
            -i "${RUNNER_TEMP}/hosts.yml" \
            deploy/ansible/deploy.yml \
            --extra-vars "@${RUNTIME_VARS_FILE}"

      - name: Post-deploy health, checks, and metrics sanity
        run: |
          set -euo pipefail
          https_port="${MAXBRIDGE_HTTPS_PORT:-8443}"
          base_url="https://${MAXBRIDGE_DOMAIN}:${https_port}"

          curl -ksSf "${base_url}/health/live" >/dev/null
          curl -ksSf "${base_url}/health/ready" >/dev/null
          checks="$(curl -ksSf "${base_url}/health/checks")"
          printf '%s' "${checks}" | jq -e '
            (.queue | type == "object") and
            (.queue.pending | type == "number") and
            (.queue.retry | type == "number") and
            (.queue.dead_letter | type == "number")
          ' >/dev/null

          metrics_tmp="${RUNNER_TEMP}/metrics-staging.out"
          metrics_status="$(curl -ksS -o "${metrics_tmp}" -w '%{http_code}' "${base_url}/metrics" || true)"
          if [ "${metrics_status}" = "200" ]; then
            metrics="$(cat "${metrics_tmp}")"
            printf '%s' "${metrics}" | grep -q '^queue_depth '
            printf '%s' "${metrics}" | grep -q '^telegram_updates_total '
          elif [ "${metrics_status}" = "401" ] || [ "${metrics_status}" = "403" ]; then
            echo "Metrics endpoint is protected (${metrics_status}); skipping public metrics assertions."
          else
            echo "Unexpected HTTP status from /metrics: ${metrics_status}"
            exit 1
          fi
          rm -f "${metrics_tmp}"

      - name: Publish summary
        run: |
          {
            echo "### Staging deployment completed"
            echo
            echo "Environment: ${DEPLOY_ENV}"
            echo "Image: ${IMAGE_REPO}:${IMAGE_TAG}"
            echo "Domain: ${MAXBRIDGE_DOMAIN}"
          } >> "${GITHUB_STEP_SUMMARY}"

      - name: Cleanup sensitive runtime files
        if: always()
        run: |
          rm -f "${RUNNER_TEMP}/ansible-runtime-vars.json"
          rm -f "${RUNNER_TEMP}/ansible-runtime-vars.json.tmp"

  deploy-production:
    if: ${{ needs.prepare.outputs.promote_to_production == 'true' }}
    needs: [prepare, deploy-staging]
    runs-on: [self-hosted, Linux, X64, wsl-deploy]
    environment: production
    env:
      DEPLOY_ENV: production
      IMAGE_TAG: ${{ needs.prepare.outputs.image_tag }}
      IMAGE_REPO: ${{ vars.MAXBRIDGE_IMAGE_REPO }}
      MAXBRIDGE_REGISTRY_USERNAME: ${{ vars.MAXBRIDGE_REGISTRY_USERNAME }}
      MAXBRIDGE_DOMAIN: ${{ vars.MAXBRIDGE_DOMAIN }}
      MAXBRIDGE_HTTPS_PORT: ${{ vars.MAXBRIDGE_HTTPS_PORT }}
      DEPLOY_HOST: ${{ vars.DEPLOY_HOST }}
      DEPLOY_USER: ${{ vars.DEPLOY_USER }}
      DEPLOY_SSH_KEY_PATH: ${{ vars.DEPLOY_SSH_KEY_PATH }}
      DEPLOY_SSH_KNOWN_HOSTS_PATH: ${{ vars.DEPLOY_SSH_KNOWN_HOSTS_PATH }}
      MAXBRIDGE_AUTO_BOOTSTRAP: ${{ vars.MAXBRIDGE_AUTO_BOOTSTRAP }}
      MAXBRIDGE_TELEGRAM_BOT_TOKEN: ${{ secrets.MAXBRIDGE_TELEGRAM_BOT_TOKEN }}
      MAXBRIDGE_MAX_BOT_TOKEN: ${{ secrets.MAXBRIDGE_MAX_BOT_TOKEN }}
      MAXBRIDGE_REGISTRY_TOKEN: ${{ secrets.MAXBRIDGE_REGISTRY_TOKEN }}
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Validate deploy configuration
        run: |
          set -euo pipefail

          missing=0
          for var_name in IMAGE_REPO DEPLOY_HOST DEPLOY_USER IMAGE_TAG MAXBRIDGE_DOMAIN MAXBRIDGE_TELEGRAM_BOT_TOKEN MAXBRIDGE_MAX_BOT_TOKEN MAXBRIDGE_REGISTRY_TOKEN; do
            if [ -z "${!var_name}" ]; then
              echo "Missing required value: ${var_name}"
              missing=1
            fi
          done

          if [ "${missing}" -ne 0 ]; then
            exit 1
          fi

          if [ "${IMAGE_TAG}" = "main" ]; then
            echo "image_tag=main is not allowed for deploy. Use immutable tags only (sha-<12hex> or v*)."
            exit 1
          fi

          if [[ ! "${IMAGE_TAG}" =~ ^sha-[0-9a-f]{12}$ && ! "${IMAGE_TAG}" =~ ^v[0-9A-Za-z._-]+$ ]]; then
            echo "Unsupported image_tag format: ${IMAGE_TAG}"
            echo "Allowed formats: sha-<12hex> or v* release tag."
            exit 1
          fi

      - name: Validate local SSH material on self-hosted runner
        run: |
          set -euo pipefail
          ssh_key_path="${DEPLOY_SSH_KEY_PATH:-$HOME/.ssh/maxbridge_deploy}"
          known_hosts_path="${DEPLOY_SSH_KNOWN_HOSTS_PATH:-$HOME/.ssh/known_hosts}"

          if [ ! -f "${ssh_key_path}" ]; then
            echo "Deploy SSH key is missing on runner: ${ssh_key_path}"
            exit 1
          fi

          if [ ! -f "${known_hosts_path}" ]; then
            echo "known_hosts file is missing on runner: ${known_hosts_path}"
            exit 1
          fi

          chmod 600 "${ssh_key_path}"
          chmod 600 "${known_hosts_path}"

          echo "SSH_KEY_PATH=${ssh_key_path}" >> "${GITHUB_ENV}"
          echo "KNOWN_HOSTS_PATH=${known_hosts_path}" >> "${GITHUB_ENV}"

      - name: Ensure toolchain is available
        run: |
          set -euo pipefail

          if ! command -v jq >/dev/null 2>&1; then
            echo "jq is required on self-hosted runner."
            exit 1
          fi

          if ! command -v ansible-playbook >/dev/null 2>&1; then
            echo "ansible-playbook is missing. Pre-provision Ansible on the self-hosted runner."
            exit 1
          fi

          if ! command -v ansible-galaxy >/dev/null 2>&1; then
            echo "ansible-galaxy is missing. Install full Ansible distribution on the self-hosted runner."
            exit 1
          fi

          ansible-galaxy collection install community.docker

      - name: Build runtime vars for Ansible
        run: |
          set -euo pipefail

          echo "::add-mask::${MAXBRIDGE_TELEGRAM_BOT_TOKEN}"
          echo "::add-mask::${MAXBRIDGE_MAX_BOT_TOKEN}"
          echo "::add-mask::${MAXBRIDGE_REGISTRY_TOKEN}"

          runtime_vars_file="${RUNNER_TEMP}/ansible-runtime-vars.json"
          https_port="${MAXBRIDGE_HTTPS_PORT:-8443}"
          registry_username="${MAXBRIDGE_REGISTRY_USERNAME}"

          if [ -z "${registry_username}" ]; then
            first_segment="${IMAGE_REPO%%/*}"
            if [[ "${first_segment}" == *.* || "${first_segment}" == *:* || "${first_segment}" == "localhost" ]]; then
              remainder="${IMAGE_REPO#*/}"
              registry_username="${remainder%%/*}"
            else
              registry_username="${first_segment}"
            fi
          fi

          if [ -z "${registry_username}" ] || [ "${registry_username}" = "${IMAGE_REPO}" ]; then
            echo "Unable to resolve registry username. Set MAXBRIDGE_REGISTRY_USERNAME variable."
            exit 1
          fi

          jq -n \
            --arg version "${IMAGE_TAG}" \
            --arg image "${IMAGE_REPO}:${IMAGE_TAG}" \
            --arg domain "${MAXBRIDGE_DOMAIN}" \
            --arg https_port "${https_port}" \
            --arg health_url "https://${MAXBRIDGE_DOMAIN}:${https_port}/health/ready" \
            --arg registry_username "${registry_username}" \
            --arg telegram_token "${MAXBRIDGE_TELEGRAM_BOT_TOKEN}" \
            --arg max_token "${MAXBRIDGE_MAX_BOT_TOKEN}" \
            --arg registry_token "${MAXBRIDGE_REGISTRY_TOKEN}" \
            '{
              maxbridge_version: $version,
              maxbridge_image: $image,
              maxbridge_domain: $domain,
              maxbridge_https_port: ($https_port | tonumber),
              maxbridge_healthcheck_url: $health_url,
              maxbridge_registry_private: true,
              maxbridge_registry_username: $registry_username,
              maxbridge_telegram_bot_token: $telegram_token,
              maxbridge_max_bot_token: $max_token,
              maxbridge_registry_token: $registry_token
            }' > "${runtime_vars_file}"

          chmod 600 "${runtime_vars_file}"
          echo "RUNTIME_VARS_FILE=${runtime_vars_file}" >> "${GITHUB_ENV}"

      - name: Render CI inventory
        run: |
          set -euo pipefail
          cat > "${RUNNER_TEMP}/hosts.yml" <<EOF_HOSTS
          all:
            hosts:
              maxbridge-${DEPLOY_ENV}-1:
                ansible_host: ${DEPLOY_HOST}
                ansible_user: ${DEPLOY_USER}
                ansible_ssh_private_key_file: ${SSH_KEY_PATH}
                ansible_ssh_common_args: '-o StrictHostKeyChecking=yes -o UserKnownHostsFile=${KNOWN_HOSTS_PATH}'
          EOF_HOSTS

      - name: Optional bootstrap
        if: ${{ vars.MAXBRIDGE_AUTO_BOOTSTRAP == 'true' }}
        env:
          ANSIBLE_HOST_KEY_CHECKING: "true"
        run: |
          set -euo pipefail
          ansible-playbook \
            -i "${RUNNER_TEMP}/hosts.yml" \
            deploy/ansible/bootstrap.yml

      - name: Deploy image with Ansible
        env:
          ANSIBLE_HOST_KEY_CHECKING: "true"
        run: |
          set -euo pipefail
          ansible-playbook \
            -i "${RUNNER_TEMP}/hosts.yml" \
            deploy/ansible/deploy.yml \
            --extra-vars "@${RUNTIME_VARS_FILE}"

      - name: Post-deploy health, checks, and metrics sanity
        run: |
          set -euo pipefail
          https_port="${MAXBRIDGE_HTTPS_PORT:-8443}"
          base_url="https://${MAXBRIDGE_DOMAIN}:${https_port}"

          curl -ksSf "${base_url}/health/live" >/dev/null
          curl -ksSf "${base_url}/health/ready" >/dev/null
          checks="$(curl -ksSf "${base_url}/health/checks")"
          printf '%s' "${checks}" | jq -e '
            (.queue | type == "object") and
            (.queue.pending | type == "number") and
            (.queue.retry | type == "number") and
            (.queue.dead_letter | type == "number")
          ' >/dev/null

          metrics_tmp="${RUNNER_TEMP}/metrics-production.out"
          metrics_status="$(curl -ksS -o "${metrics_tmp}" -w '%{http_code}' "${base_url}/metrics" || true)"
          if [ "${metrics_status}" = "200" ]; then
            metrics="$(cat "${metrics_tmp}")"
            printf '%s' "${metrics}" | grep -q '^queue_depth '
            printf '%s' "${metrics}" | grep -q '^telegram_updates_total '
          elif [ "${metrics_status}" = "401" ] || [ "${metrics_status}" = "403" ]; then
            echo "Metrics endpoint is protected (${metrics_status}); skipping public metrics assertions."
          else
            echo "Unexpected HTTP status from /metrics: ${metrics_status}"
            exit 1
          fi
          rm -f "${metrics_tmp}"

      - name: Publish summary
        run: |
          {
            echo "### Production deployment completed"
            echo
            echo "Environment: ${DEPLOY_ENV}"
            echo "Image: ${IMAGE_REPO}:${IMAGE_TAG}"
            echo "Domain: ${MAXBRIDGE_DOMAIN}"
          } >> "${GITHUB_STEP_SUMMARY}"

      - name: Cleanup sensitive runtime files
        if: always()
        run: |
          rm -f "${RUNNER_TEMP}/ansible-runtime-vars.json"
          rm -f "${RUNNER_TEMP}/ansible-runtime-vars.json.tmp"

  auto-rollback-production:
    if: ${{ always() && needs.prepare.outputs.promote_to_production == 'true' && needs.deploy-production.result == 'failure' }}
    needs: [prepare, deploy-production]
    runs-on: [self-hosted, Linux, X64, wsl-deploy]
    environment: production
    env:
      DEPLOY_ENV: production
      IMAGE_TAG: ${{ needs.prepare.outputs.rollback_tag }}
      IMAGE_REPO: ${{ vars.MAXBRIDGE_IMAGE_REPO }}
      MAXBRIDGE_REGISTRY_USERNAME: ${{ vars.MAXBRIDGE_REGISTRY_USERNAME }}
      MAXBRIDGE_DOMAIN: ${{ vars.MAXBRIDGE_DOMAIN }}
      MAXBRIDGE_HTTPS_PORT: ${{ vars.MAXBRIDGE_HTTPS_PORT }}
      DEPLOY_HOST: ${{ vars.DEPLOY_HOST }}
      DEPLOY_USER: ${{ vars.DEPLOY_USER }}
      DEPLOY_SSH_KEY_PATH: ${{ vars.DEPLOY_SSH_KEY_PATH }}
      DEPLOY_SSH_KNOWN_HOSTS_PATH: ${{ vars.DEPLOY_SSH_KNOWN_HOSTS_PATH }}
      MAXBRIDGE_TELEGRAM_BOT_TOKEN: ${{ secrets.MAXBRIDGE_TELEGRAM_BOT_TOKEN }}
      MAXBRIDGE_MAX_BOT_TOKEN: ${{ secrets.MAXBRIDGE_MAX_BOT_TOKEN }}
      MAXBRIDGE_REGISTRY_TOKEN: ${{ secrets.MAXBRIDGE_REGISTRY_TOKEN }}
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Validate rollback configuration
        run: |
          set -euo pipefail

          missing=0
          for var_name in IMAGE_REPO DEPLOY_HOST DEPLOY_USER IMAGE_TAG MAXBRIDGE_DOMAIN MAXBRIDGE_TELEGRAM_BOT_TOKEN MAXBRIDGE_MAX_BOT_TOKEN MAXBRIDGE_REGISTRY_TOKEN; do
            if [ -z "${!var_name}" ]; then
              echo "Missing required value: ${var_name}"
              missing=1
            fi
          done

          if [ "${missing}" -ne 0 ]; then
            exit 1
          fi

      - name: Validate local SSH material on self-hosted runner
        run: |
          set -euo pipefail
          ssh_key_path="${DEPLOY_SSH_KEY_PATH:-$HOME/.ssh/maxbridge_deploy}"
          known_hosts_path="${DEPLOY_SSH_KNOWN_HOSTS_PATH:-$HOME/.ssh/known_hosts}"

          if [ ! -f "${ssh_key_path}" ]; then
            echo "Deploy SSH key is missing on runner: ${ssh_key_path}"
            exit 1
          fi

          if [ ! -f "${known_hosts_path}" ]; then
            echo "known_hosts file is missing on runner: ${known_hosts_path}"
            exit 1
          fi

          chmod 600 "${ssh_key_path}"
          chmod 600 "${known_hosts_path}"

          echo "SSH_KEY_PATH=${ssh_key_path}" >> "${GITHUB_ENV}"
          echo "KNOWN_HOSTS_PATH=${known_hosts_path}" >> "${GITHUB_ENV}"

      - name: Ensure toolchain is available
        run: |
          set -euo pipefail

          if ! command -v jq >/dev/null 2>&1; then
            echo "jq is required on self-hosted runner."
            exit 1
          fi

          if ! command -v ansible-playbook >/dev/null 2>&1; then
            echo "ansible-playbook is missing. Pre-provision Ansible on the self-hosted runner."
            exit 1
          fi

          if ! command -v ansible-galaxy >/dev/null 2>&1; then
            echo "ansible-galaxy is missing. Install full Ansible distribution on the self-hosted runner."
            exit 1
          fi

          ansible-galaxy collection install community.docker

      - name: Build runtime vars for Ansible
        run: |
          set -euo pipefail

          echo "::add-mask::${MAXBRIDGE_TELEGRAM_BOT_TOKEN}"
          echo "::add-mask::${MAXBRIDGE_MAX_BOT_TOKEN}"
          echo "::add-mask::${MAXBRIDGE_REGISTRY_TOKEN}"

          runtime_vars_file="${RUNNER_TEMP}/ansible-runtime-vars.json"
          https_port="${MAXBRIDGE_HTTPS_PORT:-8443}"
          registry_username="${MAXBRIDGE_REGISTRY_USERNAME}"

          if [ -z "${registry_username}" ]; then
            first_segment="${IMAGE_REPO%%/*}"
            if [[ "${first_segment}" == *.* || "${first_segment}" == *:* || "${first_segment}" == "localhost" ]]; then
              remainder="${IMAGE_REPO#*/}"
              registry_username="${remainder%%/*}"
            else
              registry_username="${first_segment}"
            fi
          fi

          if [ -z "${registry_username}" ] || [ "${registry_username}" = "${IMAGE_REPO}" ]; then
            echo "Unable to resolve registry username. Set MAXBRIDGE_REGISTRY_USERNAME variable."
            exit 1
          fi

          jq -n \
            --arg version "${IMAGE_TAG}" \
            --arg image "${IMAGE_REPO}:${IMAGE_TAG}" \
            --arg domain "${MAXBRIDGE_DOMAIN}" \
            --arg https_port "${https_port}" \
            --arg health_url "https://${MAXBRIDGE_DOMAIN}:${https_port}/health/ready" \
            --arg registry_username "${registry_username}" \
            --arg telegram_token "${MAXBRIDGE_TELEGRAM_BOT_TOKEN}" \
            --arg max_token "${MAXBRIDGE_MAX_BOT_TOKEN}" \
            --arg registry_token "${MAXBRIDGE_REGISTRY_TOKEN}" \
            '{
              maxbridge_version: $version,
              maxbridge_image: $image,
              maxbridge_domain: $domain,
              maxbridge_https_port: ($https_port | tonumber),
              maxbridge_healthcheck_url: $health_url,
              maxbridge_registry_private: true,
              maxbridge_registry_username: $registry_username,
              maxbridge_telegram_bot_token: $telegram_token,
              maxbridge_max_bot_token: $max_token,
              maxbridge_registry_token: $registry_token
            }' > "${runtime_vars_file}"

          chmod 600 "${runtime_vars_file}"
          echo "RUNTIME_VARS_FILE=${runtime_vars_file}" >> "${GITHUB_ENV}"

      - name: Render CI inventory
        run: |
          set -euo pipefail
          cat > "${RUNNER_TEMP}/hosts.yml" <<EOF_HOSTS
          all:
            hosts:
              maxbridge-${DEPLOY_ENV}-1:
                ansible_host: ${DEPLOY_HOST}
                ansible_user: ${DEPLOY_USER}
                ansible_ssh_private_key_file: ${SSH_KEY_PATH}
                ansible_ssh_common_args: '-o StrictHostKeyChecking=yes -o UserKnownHostsFile=${KNOWN_HOSTS_PATH}'
          EOF_HOSTS

      - name: Roll back with Ansible
        env:
          ANSIBLE_HOST_KEY_CHECKING: "true"
        run: |
          set -euo pipefail
          ansible-playbook \
            -i "${RUNNER_TEMP}/hosts.yml" \
            deploy/ansible/deploy.yml \
            --extra-vars "@${RUNTIME_VARS_FILE}"

      - name: Post-rollback health and metrics sanity
        run: |
          set -euo pipefail
          https_port="${MAXBRIDGE_HTTPS_PORT:-8443}"
          base_url="https://${MAXBRIDGE_DOMAIN}:${https_port}"

          curl -ksSf "${base_url}/health/ready" >/dev/null
          checks="$(curl -ksSf "${base_url}/health/checks")"
          printf '%s' "${checks}" | jq -e '
            (.queue | type == "object") and
            (.queue.pending | type == "number") and
            (.queue.retry | type == "number") and
            (.queue.dead_letter | type == "number")
          ' >/dev/null

      - name: Publish summary
        run: |
          {
            echo "### Automatic production rollback completed"
            echo
            echo "Environment: ${DEPLOY_ENV}"
            echo "Image: ${IMAGE_REPO}:${IMAGE_TAG}"
            echo "Domain: ${MAXBRIDGE_DOMAIN}"
          } >> "${GITHUB_STEP_SUMMARY}"

      - name: Cleanup sensitive runtime files
        if: always()
        run: |
          rm -f "${RUNNER_TEMP}/ansible-runtime-vars.json"
          rm -f "${RUNNER_TEMP}/ansible-runtime-vars.json.tmp"
