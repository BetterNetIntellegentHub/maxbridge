name: cd-image (deprecated)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: GitHub CD is disabled
        run: |
          echo "GitHub CD workflows are disabled."
          echo "Use GitLab pipeline (source of truth) for image build/publish."
